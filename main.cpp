#include <vector>
#include <string>
#include <iostream>
#include "Functions/functions.h"
using namespace std;
int main(){
    string command;
    bool exit = false;
    while (!exit){
        cout << "Enter A Command (use help for info):";
        cin >> command;
        if (tolower(command) == "help")
        {
            /* code */
        }
        else if (command == "")
        {
            /* code */
        }
        else if (command == "")
        {
            /* code */
        }
        else if (command == "")
        {
            /* code */
        }
        
        
    }
}